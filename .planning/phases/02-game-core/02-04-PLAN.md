---
phase: 02-game-core
plan: '04'
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/app/game/[roomId]/page.tsx
autonomous: true
gap_closure: true
requirements: [DEP-01, DEP-04]

must_haves:
  truths:
    - "Manual piece deployment syncs to server via socket event"
    - "Second player joining sees manually deployed pieces on board"
    - "Ready button enables after 21 pieces placed manually"
  artifacts:
    - path: "client/src/app/game/[roomId]/page.tsx"
      provides: "socket.emit('deploy-piece') after deployPiece() call"
      min_lines: 1
  key_links:
    - from: "client/src/app/game/[roomId]/page.tsx"
      to: "server/src/socket/handlers/gameHandler.ts"
      via: "socket.emit('deploy-piece', { pieceId, row, col })"
      pattern: "socket\\.emit\\('deploy-piece'"
---

<objective>
Fix the missing `deploy-piece` socket emission in the game page. After calling `deployPiece()` locally for optimistic UI update, emit `socket.emit('deploy-piece', { pieceId, row, col })` to sync with server. This resolves DEP-01 and DEP-04 being blocked.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@client/src/app/game/[roomId]/page.tsx

The handleCellClick function for the deploying phase (lines 55-76) calls deployPiece() locally but never emits the socket event. The pieceId is derived from the piece object created at line 66-72.

**Current code (lines 74-76):**
```typescript
deployPiece(piece, { row, col });
setSelectedPieceType(null);
return;
```

**Expected code:**
```typescript
deployPiece(piece, { row, col });
socket?.emit('deploy-piece', { pieceId: piece.id, row, col });
setSelectedPieceType(null);
return;
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deploy-piece socket emission after deployPiece()</name>
  <files>client/src/app/game/[roomId]/page.tsx</files>
  <action>
    In handleCellClick, after the `deployPiece(piece, { row, col });` line inside the deploying phase block (around line 74), add:

    ```typescript
    socket?.emit('deploy-piece', { pieceId: piece.id, row, col });
    ```

    This syncs the manually deployed piece to the server so it appears for all players and populates deployedPieces on the server for Ready button validation.
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit src/app/game/\[roomId\]/page.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Manual piece deployment emits socket event; second player joining sees deployed pieces; Ready button works after 21 manual placements</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Manual deployment now emits deploy-piece event
- DEP-01 and DEP-04 are unblocked
</verification>

<success_criteria>
socket.emit('deploy-piece') is present in handleCellClick after deployPiece() call
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-core/02-04-SUMMARY.md`
</output>
