---
phase: 02-game-core
plan: '03'
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - client/src/store/gameStore.ts
  - client/src/features/game/Board.tsx
  - client/src/features/game/Piece.tsx
  - client/src/features/game/PiecePalette.tsx
  - client/src/app/game/[roomId]/page.tsx
  - client/src/features/game/BattleReveal.tsx
autonomous: false
requirements:
  - DEP-01
  - DEP-03
  - DEP-04
  - GAME-02
  - GAME-03
  - GAME-07
user_setup: []
must_haves:
  truths:
    - "Deployment: clicking board square places selected palette piece after server validation"
    - "Auto-Deploy button places all 21 pieces instantly, can re-randomize"
    - "Ready button enabled when 21 pieces placed; countdown starts when both ready"
    - "Playing phase: clicking own piece shows gold border selection"
    - "Valid moves highlighted green when piece selected"
    - "Battle reveal animation plays inline (~1s) showing attacker vs defender"
  artifacts:
    - path: client/src/store/gameStore.ts
      provides: Extended state with selectedPiece, validMoves, makeMove, ready state
    - path: client/src/features/game/Board.tsx
      provides: Valid move highlighting (green squares)
    - path: client/src/features/game/Piece.tsx
      provides: Gold selection border, red flash on opponent piece click
    - path: client/src/features/game/BattleReveal.tsx
      provides: Inline battle reveal animation component
    - path: client/src/app/game/[roomId]/page.tsx
      provides: Auto-Deploy button, Ready button, playing-phase handlers
  key_links:
    - from: client/src/app/game/[roomId]/page.tsx
      to: client/src/store/gameStore.ts
      via: socket event handlers call store actions
      pattern: socket.on.*piece:deployed|socket.on.*move:result
    - from: client/src/features/game/Board.tsx
      to: client/src/store/gameStore.ts
      via: reads validMoves and selectedPiece
      pattern: useGameStore.*validMoves|useGameStore.*selectedPiece
---

<objective>
Implement all client-side game features: extended gameStore, updated Board/Piece components, BattleReveal animation, and game page with Auto-Deploy/Ready buttons and playing-phase handlers. Human verification at end.
</objective>

<context>
@client/src/store/gameStore.ts
@client/src/features/game/Board.tsx
@client/src/features/game/Piece.tsx
@client/src/features/game/PiecePalette.tsx
@client/src/app/game/[roomId]/page.tsx
@02-UI-SPEC.md
@02-CONTEXT.md

# Key visual specs from 02-UI-SPEC.md:
# - Selected piece border: #d4a847 (gold), 2px solid
# - Valid move highlight: rgba(74, 124, 74, 0.5) green-tinted
# - Invalid click flash: rgba(192, 57, 43, 0.6) red, 200ms
# - Equal rank explosion: rgba(255, 100, 50, 0.8) burst + spark particles
# - Ready button: bg-[#d4a847] accent gold, enabled when 21 pieces
# - Auto-Deploy: secondary style below palette, no animation
# - Turn indicator: "Your turn" / "Waiting for opponent…"

# Socket events client listens for:
# - piece:deployed: { piece, row, col, deployedCount, board, autoDeployComplete? }
# - player:ready: { playerId }
# - deploy:complete: { board, currentTurn: 'red' }
# - move:result: { move: { from, to }, outcome, board, currentTurn }
# - countdown:update: { seconds: number }
# - error: { message: string }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend client gameStore with playing-phase state</name>
  <files>client/src/store/gameStore.ts</files>
  <read_first>
    client/src/store/gameStore.ts
    client/src/types/index.ts
  </read_first>
  <action>
    Extend the GameState interface and store implementation:

    Add to interface:
    ```typescript
    validMoves: Position[];
    selectedPiece: Position | null;
    readyToPlay: boolean;
    opponentReady: boolean;
    countdownSeconds: number | null;
    battleOutcome: BattleOutcome | null;
    playerReady: boolean;
    ```

    Add new actions:
    ```typescript
    // In selectPiece: compute valid moves for selected own piece during 'playing' phase
    selectPiece: (pos: Position | null) => void;
    // Should: if pos is null, clear selection. If pos has own piece during own turn, compute validMoves using getValidMoves logic. Else clear.

    // Move execution (optimistic)
    makeMove: (from: Position, to: Position) => void;
    // Should: optimistically update board, then emit 'make-move' socket event

    // Ready state
    setReady: () => void;
    // Should: emit 'ready' socket event, set playerReady: true

    // Battle outcome display
    setBattleOutcome: (outcome: BattleOutcome | null) => void;
    clearBattleOutcome: () => void;
    ```

    Add a helper for computing valid moves (can be a non-exported function or inline):
    - From board + position + playerSide, return array of valid {row, col} destinations
    - Orthogonal only, within bounds, not occupied by own piece
    - Include enemy-occupied squares (for battle)

    Also import and re-export BattleOutcome type if needed.
  </action>
  <verify>
    <automated>grep -c "validMoves\|selectPiece\|makeMove\|setReady\|setBattleOutcome" client/src/store/gameStore.ts</automated>
  </verify>
  <done>gameStore extended with validMoves, selectedPiece, makeMove, setReady, setBattleOutcome, battleOutcome, playerReady, opponentReady, countdownSeconds. selectPiece computes valid moves during playing phase.</done>
</task>

<task type="auto">
  <name>Task 2: Update Board and Piece components with game interactions</name>
  <files>
    client/src/features/game/Board.tsx
    client/src/features/game/Piece.tsx
  </files>
  <read_first>
    client/src/features/game/Board.tsx
    client/src/features/game/Piece.tsx
    client/src/store/gameStore.ts
  </read_first>
  <action>
    **Board.tsx updates:**

    1. Import from gameStore: board, selectedPiece, validMoves, currentTurn, playerSide, gameStatus, battleOutcome
    2. When rendering each cell, check if its position is in validMoves array
       - If valid move: add overlay div with `bg-[rgba(74,124,74,0.5)]` class
    3. When rendering each cell, check if its position equals selectedPiece
       - If selected: the cell gets no extra class (piece handles its own border)
    4. For deployment phase: DeploymentZone overlay should highlight valid zone in green (rgba(74, 124, 74, 0.35)) when a palette piece type is selected (pass selectedPieceType prop down or check store)
    5. Handle cell clicks:
       - Playing phase + validMoves includes this cell → call makeMove
       - Playing phase + own piece → selectPiece
       - Otherwise → deselect
    6. Add turn indicator: display "Your turn" or "Waiting for opponent…" in a header above the board
       - If currentTurn === playerSide: "Your turn" (gold text)
       - Else: "Waiting for opponent…" (gray text)

    **Piece.tsx updates:**

    1. Add `isSelected?: boolean` prop
    2. Add `onInvalidClick?: () => void` prop (for opponent piece click → red flash)
    3. When isSelected is true: add `ring-2 ring-[#d4a847]` and `ring-2` to the button classes
    4. Add red flash animation: when onInvalidClick fires, add a brief red overlay (200ms) using state or CSS animation
       - Use a useState for flash state: const [flashing, setFlashing] = useState(false)
       - On click: call onInvalidClick() then setFlashing(true) then setTimeout(200ms) → setFlashing(false)
    5. For playing phase: the Piece onClick should be handled by the parent (Board) which decides based on piece ownership
  </action>
  <verify>
    <automated>grep -c "validMoves\|ring-\[#d4a847\]\|rgba(74, 124, 74" client/src/features/game/Board.tsx client/src/features/game/Piece.tsx</automated>
  </verify>
  <done>Board shows green valid-move highlights. Piece shows gold border when selected. Opponent piece click triggers red flash. Turn indicator shows above board.</done>
</task>

<task type="auto">
  <name>Task 3: Create BattleReveal animation component</name>
  <files>client/src/features/game/BattleReveal.tsx</files>
  <read_first>
    02-UI-SPEC.md (battle reveal section)
    client/src/features/game/Piece.tsx
  </read_first>
  <action>
    Create `client/src/features/game/BattleReveal.tsx` component:

    Props:
    ```typescript
    interface BattleRevealProps {
      attacker: Piece;
      defender: Piece;
      attackerPosition: Position;
      defenderPosition: Position;
      outcome: BattleOutcome;
      onComplete: () => void;
    }
    ```

    Behavior:
    1. Shows two pieces side by side (or overlapping toward each other)
    2. Pieces slide together from their positions toward center (~500ms CSS transition)
    3. Both pieces revealed (show rank icons)
    4. After ~1 second total:
       - If attacker wins: defender fades out, attacker stays
       - If defender wins: attacker fades out, defender stays
       - If tie: both fade out with explosion/spark effect
    5. Explosion effect for tie: radial gradient burst `rgba(255, 100, 50, 0.8)` with 6-8 spark particles radiating outward
       - CSS animation: @keyframes explosion { scale 0→1.5, opacity 1→0 }
       - Sparks: small circles with CSS animation rotating outward
    6. After animation: call onComplete()

    Implementation approach:
    - Use absolute positioning to place attacker and defender
    - CSS transforms to slide them together
    - CSS keyframe animations for the explosion effect
    - Use setTimeout to sequence reveal → result phases
  </action>
  <verify>
    <automated>grep -c "explosion\|BattleReveal\|attacker\|defender" client/src/features/game/BattleReveal.tsx</automated>
  </verify>
  <done>BattleReveal.tsx exists with sliding animation, reveal phase, and tie explosion effect.</done>
</task>

<task type="auto">
  <name>Task 4: Update game page with Auto-Deploy, Ready buttons, and socket handlers</name>
  <files>client/src/app/game/[roomId]/page.tsx</files>
  <read_first>
    client/src/app/game/[roomId]/page.tsx
    client/src/store/gameStore.ts
    client/src/features/game/Board.tsx
    client/src/features/game/PiecePalette.tsx
  </read_first>
  <action>
    Update the game page to handle all game phases:

    **Socket handlers (useEffect):**
    
    1. `piece:deployed`: update board from server payload, update deployedCounts
    2. `player:ready`: set opponentReady: true in store
    3. `deploy:complete`: set gameStatus: 'playing', setBoard from payload, currentTurn: 'red'
    4. `move:result`: set board from payload, setBattleOutcome from payload, setTurn(payload.currentTurn)
    5. `countdown:update`: set countdownSeconds
    6. `error`: show error toast (use alert or a simple state-based message)

    **Deployment changes:**
    - Change handleCellClick to emit `deploy-piece` socket event instead of directly calling deployPiece
    - Keep local deployPiece as fallback for optimistic update

    **Add Auto-Deploy button** (below PiecePalette):
    - Label: "Auto-Deploy"
    - Style: secondary (not accent gold) — gray background, hover bg-gray-600
    - On click: emit `auto-deploy` socket event

    **Add Ready button** (below PiecePalette, to the right of Auto-Deploy):
    - Label: "Ready" when enabled, "Ready ✓" when clicked
    - Style: bg-[#d4a847] accent gold, full width
    - Enabled: when all 21 pieces placed (deployedCounts shows count === 21)
    - On click: emit `ready` socket event, set playerReady: true, disable palette interaction
    - After clicked: show "Ready ✓" and disable button

    **Playing phase UI:**
    - When gameStatus === 'playing': show playing-phase Board (with valid moves, selection)
    - PiecePalette hidden during playing phase
    - Show turn indicator in Board

    **Countdown display:**
    - When countdownSeconds !== null: show overlay/text "Game starting in {seconds}…"
    - Large centered text with gold color

    **Battle reveal:**
    - When battleOutcome is set: render BattleReveal component
    - After animation completes: clearBattleOutcome()

    Refactor handleCellClick to support both deployment and playing phases:
    - Deployment: place selected palette piece (emit deploy-piece)
    - Playing: if clicking own piece → select; if clicking valid move → makeMove
  </action>
  <verify>
    <automated>grep -c "auto-deploy\|ready\|socket.on\|BattleReveal\|countdown" client/src/app/game/\[roomId\]/page.tsx</automated>
  </verify>
  <done>Game page handles all phases: deployment with Auto-Deploy/Ready buttons, playing phase with selection/moves, countdown overlay, battle reveal integration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification of game flow</name>
  <files>client/src/app/game/[roomId]/page.tsx</files>
  <action-built>Complete game flow: deployment → auto-deploy → ready → countdown → playing → battle reveal</action-built>
  <how-to-verify>
    1. Start two browser tabs (or one tab + bot) navigating to the game page
    2. Verify deployment zone highlights green when palette piece selected
    3. Click a piece in palette, click valid board square → piece places (server-validated)
    4. Click Auto-Deploy → all 21 pieces placed instantly
    5. Click Ready → button shows "Ready ✓", opponent sees "Opponent Ready"
    6. Once both ready: countdown "3… 2… 1…" appears
    7. After countdown: game status changes to "playing", Red's turn
    8. Click own piece → gold border highlight
    9. Valid moves shown as green squares
    10. Click valid square → piece moves (server-validated)
    11. Click opponent piece → brief red flash
    12. Move piece into enemy → battle reveal animation plays (~1s)
    13. Result applied after animation
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Board shows green valid-move squares when piece selected
- Piece shows gold border when selected
- Auto-Deploy button places 21 pieces instantly
- Ready button enabled at 21 pieces, emits socket event
- Countdown displays "3… 2… 1…" when both ready
- Battle reveal animates attacker vs defender inline
- Equal rank tie shows explosion effect
</verification>

<success_criteria>
- All 5 tasks complete and committed
- Human verification passed (checkpoint approved)
- gameStore extended with all playing-phase state
- Board shows valid moves in green, selection in gold
- BattleReveal component handles tie explosion
- Auto-Deploy and Ready buttons functional
- Socket events properly wired (deploy → ready → playing → battle)
</success_criteria>

<output>
After completion, create `.planning/phases/02-game-core/02-03-SUMMARY.md` using template.
</output>
