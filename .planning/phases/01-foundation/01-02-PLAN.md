---
phase: 01-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - GS-01
  - GS-02
  - GS-03
  - GS-04

files_modified:
  - client/src/features/game/Board.tsx
  - client/src/features/game/Piece.tsx
  - client/src/features/game/PiecePalette.tsx
  - client/src/features/game/DeploymentZone.tsx
  - client/src/app/game/[roomId]/page.tsx
  - client/src/app/lobby/page.tsx
  - server/src/socket/handlers/roomHandler.ts

must_haves:
  truths:
    - "Board renders as 9x8 grid with alternating green squares"
    - "Each player has 21 pieces visible in PiecePalette"
    - "Red deployment zone (rows 0-2) visually distinct with subtle red tint"
    - "Blue deployment zone (rows 5-7) visually distinct with subtle blue tint"
    - "Player can leave room and return to lobby"
    - "Room code displayed on game page"
  artifacts:
    - path: "client/src/features/game/Board.tsx"
      provides: "9x8 CSS Grid board rendering"
      exports: ["Board component with 72 squares"]
    - path: "client/src/features/game/Piece.tsx"
      provides: "Individual piece rendering with rank display"
      exports: ["Piece component with owner color, rank icon"]
    - path: "client/src/features/game/PiecePalette.tsx"
      provides: "Deployment phase piece selection"
      exports: ["PiecePalette with 21 pieces, count badges"]
    - path: "client/src/features/game/DeploymentZone.tsx"
      provides: "Deployment zone visualization"
      exports: ["DeploymentZone component with row 0-2 red, row 5-7 blue"]
  key_links:
    - from: "client/src/features/game/Board.tsx"
      to: "client/src/features/game/Piece.tsx"
      via: "Piece component rendered inside Board cells"
      pattern: "<Piece.*piece="
    - from: "client/src/app/game/[roomId]/page.tsx"
      to: "client/src/features/game/Board.tsx"
      via: "Board component imported and rendered"
      pattern: "import.*Board.*from"
    - from: "client/src/features/game/PiecePalette.tsx"
      to: "client/src/store/gameStore.ts"
      via: "useGameStore for deployedPieces count"
      pattern: "useGameStore"
---

<objective>
Implement the complete game board UI: 9x8 CSS Grid, piece rendering with rank display, piece palette for deployment, and deployment zone visualization. Complete lobby functionality with leave room. Human verification of visual components.

Purpose: Delivers the complete Phase 1 UI with board rendering, piece display, and deployment zones per GS-01 through GS-04.
Output: Playable lobby flow (create/join/leave) and visible game board with pieces and deployment zones.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md (Pattern 4)
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/PROJECT_SPECS.md (sections 3.1, 3.2, 5.3)
</context>

<interfaces>

Piece rank display mapping (from PROJECT_SPECS.md and UI-SPEC):

```typescript
const PIECE_SYMBOLS: Record<PieceType, string> = {
  '5-star': '5★',
  '4-star': '4★',
  '3-star': '3★',
  '2-star': '2★',
  '1-star': '1★',
  'colonel': 'Col',
  'lieutenant-colonel': 'LtC',
  'major': 'Maj',
  'captain': 'Cpt',
  '1st-lieutenant': '1Lt',
  '2nd-lieutenant': '2Lt',
  'sergeant': 'Sgt',
  'private': 'Pvt',
  'spy': 'Spy',
  'flag': '⚑',
};
```

Board cell colors (from 01-UI-SPEC.md):
- Light square: #4a7c4a
- Dark square: #3a6a3a
- Red deployment zone: rgba(192, 57, 43, 0.15) overlay on rows 0-2
- Blue deployment zone: rgba(41, 128, 185, 0.15) overlay on rows 5-7

Cell size: 48px × 48px desktop, 36px × 36px mobile
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Complete lobby page with leave functionality and game header</name>
  <files>
    client/src/app/lobby/page.tsx
    client/src/components/LeaveRoomButton.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-01-SUMMARY.md (if exists)
    client/src/store/roomStore.ts
    client/src/components/SocketProvider.tsx
  </read_first>
  <action>
    Update the lobby page to be fully functional:

    **Waiting State (after room creation):**
    - Display room code in large monospace font (text-4xl, tracking-widest)
    - Show "Waiting for opponent..." message
    - If isBotGame=true, show "vs Bot" badge
    - Show player name and "Red" indicator
    - "Leave Room" button that calls socket.emit('leave-room') and navigates to /

    **Two-Player State (after opponent joins):**
    - Show both player names with color indicators (red/blue)
    - Show room code
    - "Leave Room" button
    - Message: "Both players ready! Starting game..." (auto-redirect to game page)

    **Leave Room Button Component (client/src/components/LeaveRoomButton.tsx):**
    - 'use client'
    - Button with destructive styling (red background)
    - On click: emit 'leave-room', call roomStore.clearRoom(), router.push('/')
    - Confirm dialog: "Leave Room: You'll need the room code to rejoin. Are you sure?"

    **Lobby Page Updates:**
    - Parse searchParams for mode
    - Use useRouter for navigation
    - Use useSocket() for socket connection
    - Use useRoomStore for room state
    - Create/Join flow with proper socket event handlers:
      - socket.on('room:created', ({ roomId, playerId, playerSide, isBotGame }) => {...})
      - socket.on('room:joined', ({ roomId, playerId, playerSide }) => {...})
      - socket.on('player:joined', ({ player }) => {...})
      - socket.on('player:left', ({ playerId, reason }) => {...})
      - socket.on('error', ({ message }) => alert(message))
    - Navigate to /game/${roomId} when room is ready
    - Handle bot games: skip waiting for second player if isBotGame=true

    **Copywriting (from 01-UI-SPEC.md):**
    - Empty state heading: "Waiting for opponent…"
    - Empty state body: "Share the room code with a friend"
    - Destructive confirmation: "Leave Room: You'll need the room code to rejoin. Are you sure?"
  </action>
  <acceptance_criteria>
    - Lobby page displays room code in large monospace font after room creation
    - Lobby page shows "Waiting for opponent…" when waiting
    - Leave Room button emits 'leave-room' socket event
    - Leave Room button navigates to / and clears roomStore
    - Player joined/left events update UI in real-time
    - Bot games skip waiting state and proceed to game
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "leave-room" client/src/app/lobby/page.tsx &&
      grep -q "room:created" client/src/app/lobby/page.tsx &&
      grep -q "room:joined" client/src/app/lobby/page.tsx &&
      grep -q "player:joined" client/src/app/lobby/page.tsx
    </automated>
  </verify>
  <done>Lobby page fully functional with create/join/leave flow. Room code displayed. Real-time player updates.</done>
</task>

<task type="auto">
  <name>Task 2: Board component with 9x8 CSS Grid</name>
  <files>
    client/src/features/game/Board.tsx
    client/src/app/game/[roomId]/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: CSS Grid Board)
    client/src/types/index.ts
    client/src/store/gameStore.ts
  </read_first>
  <action>
    **client/src/features/game/Board.tsx:**
    - 'use client'
    - Import useGameStore
    - 9 columns × 8 rows CSS Grid using Tailwind: grid grid-cols-9 grid-rows-8
    - Board container: aspect-[9/8] (maintains 9:8 ratio), max-w-3xl, border-4 border-gray-700
    - For each cell (72 total):
      - Key: `${rowIndex}-${colIndex}`
      - Alternating colors: (rowIndex + colIndex) % 2 === 0 → bg-[#4a7c4a] (light), else bg-[#3a6a3a] (dark)
      - Cell styling: relative flex items-center justify-center aspect-square border border-gray-700
      - Render Piece component if board[rowIndex][colIndex] is not null
      - Pass piece and position to Piece component

    **client/src/app/game/[roomId]/page.tsx (Full Implementation):**
    - 'use client'
    - Import Board, useGameStore, useRoomStore, LeaveRoomButton
    - Game header section:
      - Room code display (text-xl, font-mono)
      - Player indicators (name + red/blue color badge)
      - Current status: "Waiting", "Deploying", "Playing"
      - LeaveRoomButton in top-right
    - Board section (centered):
      - DeploymentZone overlay component
      - Board component
      - PiecePalette below board (for deploying phase)
    - Game info section:
      - "Deploy Your Forces" heading during deploying phase
      - "Place all 21 pieces in your deployment zone" subtext

    **Game Header Styling:**
    - bg-[#2d4a2d] card with p-4
    - Flex layout: room code left, status center, leave button right
    - Player badges: bg-red-600 for Red, bg-blue-600 for Blue

    **Copywriting (from 01-UI-SPEC.md):**
    - Game heading (deployment): "Deploy Your Forces"
    - Game subheading (deployment): "Place all 21 pieces in your deployment zone"
    - Player colors: "Red" badge (#c0392b bg), "Blue" badge (#2980b9 bg)
  </action>
  <acceptance_criteria>
    - Board component uses CSS Grid with grid-cols-9 and grid-rows-8
    - Board has exactly 72 cells (8 rows × 9 columns)
    - Cells alternate between #4a7c4a and #3a6a3a colors
    - Board maintains 9:8 aspect ratio with max-w-3xl
    - Board renders Piece components for non-null cells
    - Game page shows room code, player names, status, leave button
    - Board centered on page with game header above
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "grid-cols-9" client/src/features/game/Board.tsx &&
      grep -q "grid-rows-8" client/src/features/game/Board.tsx &&
      grep -q "#4a7c4a" client/src/features/game/Board.tsx &&
      grep -q "#3a6a3a" client/src/features/game/Board.tsx &&
      grep -q "aspect-square" client/src/features/game/Board.tsx
    </automated>
  </verify>
  <done>Board renders 9x8 grid with alternating green colors. Game page shows header with room code and players.</done>
</task>

<task type="auto">
  <name>Task 3: Piece component, PiecePalette, and DeploymentZone</name>
  <files>
    client/src/features/game/Piece.tsx
    client/src/features/game/PiecePalette.tsx
    client/src/features/game/DeploymentZone.tsx
  </files>
  <read_first>
    client/src/types/index.ts
    client/src/store/gameStore.ts
    .planning/phases/01-foundation/01-UI-SPEC.md (piece component specs)
    .planning/PROJECT_SPECS.md (piece types and quantities)
  </read_first>
  <action>
    **client/src/features/game/Piece.tsx:**
    - 'use client'
    - Props: piece: Piece, position: Position, onClick?: () => void
    - Piece container: w-full h-full flex items-center justify-center rounded-full
    - Background: owner === 'red' → bg-red-600, owner === 'blue' → bg-blue-600
    - Rank icon in center: text-white text-xs font-bold
    - Piece symbols (from PROJECT_SPECS.md):
      ```
      '5-star': '5★', '4-star': '4★', '3-star': '3★', '2-star': '2★', '1-star': '1★',
      'colonel': 'Col', 'lieutenant-colonel': 'LtC', 'major': 'Maj',
      'captain': 'Cpt', '1st-lieutenant': '1Lt', '2nd-lieutenant': '2Lt',
      'sergeant': 'Sgt', 'private': 'Pvt', 'spy': 'Spy', 'flag': '⚑'
      ```
    - Shadow: shadow-lg
    - If not revealed: opacity-60 (grayed out)
    - Click handler for selection (if onClick provided)
    - Padding: p-1 inside cell

    **client/src/features/game/PiecePalette.tsx:**
    - 'use client'
    - Horizontal scrollable row of available pieces
    - Show one entry per piece type with count remaining
    - Use PIECE_CONFIG from @/types (21 pieces total per player)
    - Each piece item:
      - Piece preview (mini version of Piece component)
      - Count badge: bg-gray-700 rounded-full px-2 py-0.5 text-xs
      - Piece name label below
    - Selected state: ring-2 ring-[#d4a847] (gold border)
    - Group pieces by rank (generals together, officers together, others)
    - Scroll container: flex overflow-x-auto gap-2 pb-2
    - Hide pieces that are fully deployed (count === 0)

    **client/src/features/game/DeploymentZone.tsx:**
    - Props: side: 'red' | 'blue', gameStatus: GameStatus
    - Positioned overlay on board
    - Red zone (side === 'red'): rows 0-2 covered
      - bg-[rgba(192,57,43,0.15)] overlay on top 3 rows
      - opacity-50 (subtle)
    - Blue zone (side === 'blue'): rows 5-7 covered
      - bg-[rgba(41,128,185,0.15)] overlay on bottom 3 rows
      - opacity-50 (subtle)
    - Only show during 'deploying' gameStatus
    - When gameStatus === 'playing', component returns null
    - Use absolute positioning to overlay on board
    - Border: dashed border-[rgba(255,255,255,0.2)] to indicate zone

    **Integration in game page:**
    - Import DeploymentZone, PiecePalette
    - Render DeploymentZone before Board (absolute positioned)
    - Render PiecePalette below Board
    - Show/hide based on gameStatus === 'deploying'
  </action>
  <acceptance_criteria>
    - Piece component renders circular piece with owner color (red/blue)
    - Piece shows correct rank symbol (5★, Col, Pvt, ⚑, etc.)
    - PiecePalette shows all 21 pieces with count badges
    - PiecePalette shows 21 distinct piece types: 1×5★, 1×4★, 1×3★, 1×2★, 1×1★, 1×Col, 1×LtC, 1×Maj, 1×Cpt, 1×1Lt, 1×2Lt, 1×Sgt, 6×Pvt, 2×Spy, 1×⚑
    - PiecePalette count badges match PIECE_CONFIG quantities
    - DeploymentZone shows red tint on rows 0-2
    - DeploymentZone shows blue tint on rows 5-7
    - DeploymentZone only visible during deploying phase
  </acceptance_criteria>
  <verify>
    <automated>
      grep -q "bg-red-600" client/src/features/game/Piece.tsx &&
      grep -q "bg-blue-600" client/src/features/game/Piece.tsx &&
      grep -q "5★" client/src/features/game/Piece.tsx &&
      grep -q "PiecePalette" client/src/features/game/PiecePalette.tsx &&
      grep -q "rgba(192,57,43,0.15)" client/src/features/game/DeploymentZone.tsx &&
      grep -q "rgba(41,128,185,0.15)" client/src/features/game/DeploymentZone.tsx
    </automated>
  </verify>
  <done>Piece component displays correct rank icons. PiecePalette shows all 21 pieces. Deployment zones visually distinct (rows 0-2 red, rows 5-7 blue).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of board rendering and deployment zones</name>
  <action>
    Human must verify the complete Phase 1 Foundation UI:
    1. Start services: docker-compose up --build (or npm run dev in both client and server directories)
    2. Visit http://localhost:3000
    3. Verify landing page:
       - Title "Game of the Generals" in gold (#d4a847)
       - Two buttons: "Play vs Bot" and "Play Online"
       - Forest green background (#1a2e1a)
    4. Click "Play Online" → lobby page loads
    5. Enter name, click "Create Room"
    6. Verify lobby shows:
       - Room code displayed (6 alphanumeric chars, monospace font)
       - "Waiting for opponent…" message
       - "Leave Room" button (red)
    7. Open second browser/incognito, join with room code
    8. Verify both players see each other
    9. Verify game board renders:
       - 9 columns × 8 rows grid (72 cells)
       - Alternating green squares (#4a7c4a / #3a6a3a)
       - Red deployment zone (rows 0-2) with subtle red tint
       - Blue deployment zone (rows 5-7) with subtle blue tint
    10. Verify PiecePalette shows 21 pieces with counts
    11. Verify piece icons show correct rank symbols (5★, 4★, etc.)
    12. Click "Leave Room" → confirm dialog → return to landing page
  </action>
  <verify>
    <automated>Manual verification required — open browser to http://localhost:3000</automated>
  </verify>
  <done>
    Human approved: Landing page renders with mode buttons. Lobby create/join/leave works. Board renders 9x8 grid. Deployment zones visible. PiecePalette shows 21 pieces.
  </done>
  <what-built>
    Complete Phase 1 Foundation UI:
    - Landing page with Play vs Bot and Play Online buttons
    - Lobby page with create/join room, room code display, leave functionality
    - Game board: 9x8 CSS Grid with alternating green squares
    - Piece components with correct rank icons
    - PiecePalette showing all 21 pieces per player
    - Deployment zones: rows 0-2 red tint, rows 5-7 blue tint
    - Forest green theme matching UI-SPEC color palette
  </what-built>
  <how-to-verify>
    See action element above for detailed verification steps.
  </how-to-verify>
  <resume-signal>
    Type "approved" or describe any visual/functional issues that need fixing.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 01 Plan 02 verification commands:
1. Board grid: grep "grid-cols-9.*grid-rows-8" client/src/features/game/Board.tsx
2. Deployment zones: grep "rgba(192,57,43" client/src/features/game/DeploymentZone.tsx && grep "rgba(41,128,185" client/src/features/game/DeploymentZone.tsx
3. Piece counts: grep "count: 1" client/src/types/index.ts | wc -l (should be 13 lines for single-count pieces)
4. Leave room: grep "leave-room" client/src/app/lobby/page.tsx
</verification>

<success_criteria>
- Board renders 9 columns × 8 rows (GS-01: verified by count)
- Each player has 21 pieces total (GS-02: verified by PIECE_CONFIG sum)
- Red deployment zone visible on rows 0-2 (GS-03)
- Blue deployment zone visible on rows 5-7 (GS-04)
- Room creation generates 6-char codes (AUTH-01)
- Room joining works with valid codes (AUTH-02)
- Display names stored and shown (AUTH-03)
- Leave room returns to landing (AUTH-04)
- All Phase 1 requirements implemented and visually verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Files created/modified
- Visual verification results
- Issues encountered during human verification
- Screenshots if available
</output>
